apiVersion: v1
kind: Service
metadata:
  name: ngdp-service
spec:
  # type: NodePort
  selector:
    app: ngdp-pods-label
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      # nodePort: 32000
